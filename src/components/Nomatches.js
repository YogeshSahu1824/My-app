import React from 'react'

export default function Nomatches() {
    return(
        <div>NO pages found
            we can the impact of the react whenever we click on the textutils than whole the pages reload
            and wheneve we clikc on the home and textfom it don't reload
        </div>
    )

}
